<UserControl
    x:Class="Corvallis_Transit.Controls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Corvallis_Transit.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    VerticalAlignment="Top"
    VerticalContentAlignment="Top"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">

    <Grid x:Name="ControlGrid" Background="{x:Bind Background}">
        <Grid x:Name="TitleBar">
            <ContentPresenter x:Name="Content"
                              VerticalAlignment="{x:Bind VerticalContentAlignment}"
                              HorizontalAlignment="{x:Bind HorizontalContentAlignment}"
                              Margin="{x:Bind Padding}"
                              Content="{x:Bind HeaderContent}" />
        </Grid>
    </Grid>
</UserControl>
